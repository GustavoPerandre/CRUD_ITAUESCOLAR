@model IEnumerable<EscolaItau.Models.Disciplina>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de Disciplinas</h2>

<p>
    @Html.ActionLink("+ Novo Registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.titulo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.disciplina_id }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.disciplina_id }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.disciplina_id })
        </td>
    </tr>
}

</table>
